{
  "private": true,
  "name": "screenshots",
  "description": "Creates screenshots of Three.js examples",
  "eslintConfig": {
    "extends": "@wildpeaks/commonjs"
  },
  "scripts": {
    "test:setup": "webdriver-manager update",
    "test:run": "protractor protractor.conf.js",
    "test": "npm run test:setup && npm run test:run"
  },
  "devDependencies": {
    "eslint": "5.16.0",
    "protractor": "5.4.2",
    "rimraf": "2.6.3",
    "@wildpeaks/eslint-config-commonjs": "5.2.0"
  }
}
